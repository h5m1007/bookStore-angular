define(['angular'], function(angular) {
    angular.module('bookStoreDirectives', [])
        .directive('bookStoreDirective_1', ['$scope',
            function($scope) {}
        ])
        .directive('bookStoreDirective_2', ['$scope',
            function($scope) {}
        ]);
});
